console.log('🧪 TESTE BÁSICO DO SISTEMA');
console.log('Node.js está funcionando!');
console.log('Versão:', process.version);

// Teste matemático simples
const distancia = Math.sqrt(Math.pow(1.2, 2) + Math.pow(0.9, 2));
console.log('✅ Cálculo matemático:', distancia.toFixed(2));

// Teste de estrutura de dados
const estado = {
  id: 'test',
  posicao: { valence: 0.8, arousal: 0.6 }
};
console.log('✅ Estrutura de dados:', estado.id);

console.log('🎉 Sistema básico validado - pronto para Sprint 2!');
